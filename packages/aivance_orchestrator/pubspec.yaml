name: aivance_orchestrator
description: Context orchestration for Aivance Core — briefing, caching, dispatch.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  aivance_provider_contract:
    path: ../aivance_provider_contract
  github_api:
    path: ../github_api
  fpdart: ^1.1.0

dev_dependencies:
  flutter_lints: ^4.0.0
  mocktail: ^1.0.3
  test: ^1.25.0
