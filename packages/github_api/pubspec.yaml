name: github_api
description: GitHub REST API client and OAuth helpers for Cursor Mobile Commander.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: '>=3.22.0'

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
