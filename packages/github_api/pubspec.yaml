name: github_api
description: GitHub REST API client and OAuth helpers for Cursor Mobile Commander.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  dio: ^5.4.3
  fpdart: ^1.1.0

dev_dependencies:
  flutter_lints: ^4.0.0
  mocktail: ^1.0.3
  test: ^1.25.0
