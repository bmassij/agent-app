name: cursor_commander_desktop
description: Cursor Mobile Commander — Windows desktop client
publish_to: none
version: 0.1.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: '>=3.22.0'

dependencies:
  commander_orchestrator:
    path: ../../packages/commander_orchestrator
  cursor_api_agents:
    path: ../../packages/cursor_api_agents
  cursor_api_core:
    path: ../../packages/cursor_api_core
  cursor_api_stream:
    path: ../../packages/cursor_api_stream
  github_api:
    path: ../../packages/github_api
  flutter:
    sdk: flutter
  path: ^1.9.0

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
