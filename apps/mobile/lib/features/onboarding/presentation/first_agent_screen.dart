import 'package:flutter/material.dart';

/// Placeholder — Sprint 4 implements first agent creation.
class FirstAgentScreen extends StatelessWidget {
  const FirstAgentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('First Agent')),
      body: const Center(
        child: Text('Create your first agent — Sprint 4'),
      ),
    );
  }
}
